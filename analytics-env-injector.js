#!/usr/bin/env node

/**
 * Analytics Environment Injector
 * Charge les variables d'environnement et les injecte dans le thème Shopify
 */

const fs = require("fs");
const path = require("path");

// Charger dotenv si disponible
try {
  require("dotenv").config();
} catch (e) {
  console.warn(
    "dotenv non installé, utilisation des variables d'environnement système"
  );
}

const CONFIG_MAPPING = {
  GTM_CONTAINER_ID: "gtm_id",
  GA4_MEASUREMENT_ID: "ga4_id",
  GOOGLE_SITE_VERIFICATION: "google_site_verification",
  AHREFS_SITE_VERIFICATION: "ahrefs_site_verification",
  FACEBOOK_PIXEL_ID: "facebook_pixel_id",
  TURNSTILE_SITE_KEY: "turnstile_site_key",
  TURNSTILE_ENABLED: "turnstile_enabled",
  TURNSTILE_THEME: "turnstile_theme",
  TURNSTILE_SIZE: "turnstile_size",
};

/**
 * Injecte les variables d'environnement dans les snippets Liquid
 */
function injectEnvVariables() {
  console.log("🔧 Injection des variables d'environnement dans le thème...");

  // Créer le snippet de configuration
  const configSnippet = generateConfigSnippet();

  const configPath = path.join(
    __dirname,
    "snippets",
    "analytics-config.liquid"
  );
  fs.writeFileSync(configPath, configSnippet, "utf8");
  console.log("✅ Créé:", configPath);

  // Mettre à jour le layout principal
  updateThemeLayout();

  console.log("✅ Variables d'environnement injectées avec succès !");
}

/**
 * Génère le snippet de configuration Liquid
 */
function generateConfigSnippet() {
  const envValues = {};

  // Extraire les valeurs des variables d'environnement
  Object.keys(CONFIG_MAPPING).forEach((envKey) => {
    const envValue = process.env[envKey];
    if (envValue) {
      const liquidKey = CONFIG_MAPPING[envKey];
      envValues[liquidKey] = envValue;
    }
  });

  let snippet = `{% comment %}
  Analytics Configuration from Environment Variables
  Auto-generated by analytics-env-injector.js
{% endcomment %}

<script>
  // Configuration des analytics depuis les variables d'environnement
  window.analyticsEnvConfig = {
`;

  Object.entries(envValues).forEach(([key, value]) => {
    const jsValue =
      typeof value === "string" ? `'${value.replace(/'/g, "\\'")}'` : value;
    snippet += `    ${key}: ${jsValue},\n`;
  });

  snippet += `  };

  // Fusionner avec les settings Shopify
  window.shopifyThemeSettings = Object.assign(
    window.analyticsEnvConfig || {},
    window.shopifyThemeSettings || {},
    {
      // Les settings Shopify ont la priorité sur les variables d'environnement
{% for setting in settings %}
      {{ setting.key | json }}: {{ setting.value | json }},
{% endfor %}
    }
  );
</script>`;

  return snippet;
}

/**
 * Met à jour le layout principal pour inclure la configuration
 */
function updateThemeLayout() {
  const themeLayoutPath = path.join(__dirname, "layout", "theme.liquid");

  if (!fs.existsSync(themeLayoutPath)) {
    console.warn("⚠️  Fichier theme.liquid introuvable:", themeLayoutPath);
    return;
  }

  let content = fs.readFileSync(themeLayoutPath, "utf8");

  // Chercher si la configuration est déjà incluse
  if (content.includes("analytics-config")) {
    console.log("✅ Configuration déjà incluse dans theme.liquid");
    return;
  }

  // Insérer la configuration après les meta tags
  const insertAfter = "{% render 'analytics-tracking' %}";
  const insertContent = `
    {%- comment -%} Analytics Environment Configuration {%- endcomment -%}
    {% render 'analytics-config' %}`;

  if (content.includes(insertAfter)) {
    content = content.replace(insertAfter, insertAfter + insertContent);
    fs.writeFileSync(themeLayoutPath, content, "utf8");
    console.log("✅ Configuration ajoutée à theme.liquid");
  } else {
    console.warn("⚠️  Point d'insertion non trouvé dans theme.liquid");
  }
}

/**
 * Valide les variables d'environnement
 */
function validateEnvironment() {
  const errors = [];
  const warnings = [];

  const gtmId = process.env.GTM_CONTAINER_ID;
  const ga4Id = process.env.GA4_MEASUREMENT_ID;
  const facebookId = process.env.FACEBOOK_PIXEL_ID;

  // Vérifications de format
  if (gtmId && !gtmId.match(/^GTM-[A-Z0-9]+$/)) {
    errors.push("GTM_CONTAINER_ID format invalide (doit être GTM-XXXXXXX)");
  }

  if (ga4Id && !ga4Id.match(/^G-[A-Z0-9]+$/)) {
    errors.push("GA4_MEASUREMENT_ID format invalide (doit être G-XXXXXXXXXX)");
  }

  if (facebookId && !facebookId.match(/^\d{15,16}$/)) {
    errors.push(
      "FACEBOOK_PIXEL_ID format invalide (doit être un nombre de 15-16 chiffres)"
    );
  }

  // Avertissements
  if (!gtmId && !ga4Id) {
    warnings.push(
      "Aucun service d'analytics configuré (GTM_CONTAINER_ID ou GA4_MEASUREMENT_ID)"
    );
  }

  if (errors.length > 0) {
    console.error("❌ Erreurs de validation:");
    errors.forEach((error) => console.error("  -", error));
    process.exit(1);
  }

  if (warnings.length > 0) {
    console.warn("⚠️  Avertissements:");
    warnings.forEach((warning) => console.warn("  -", warning));
  }

  console.log("✅ Validation des variables d'environnement réussie");
}

/**
 * Affiche l'aide
 */
function showHelp() {
  console.log(`
Analytics Environment Injector

Usage:
  node analytics-env-injector.js [command]

Commands:
  inject     Injecte les variables d'environnement dans le thème (défaut)
  validate   Valide les variables d'environnement
  help       Affiche cette aide

Variables d'environnement supportées:
  GTM_CONTAINER_ID        ID du conteneur Google Tag Manager
  GA4_MEASUREMENT_ID      ID de mesure Google Analytics 4
  GOOGLE_SITE_VERIFICATION Code de vérification Google Search Console
  AHREFS_SITE_VERIFICATION Code de vérification Ahrefs
  FACEBOOK_PIXEL_ID       ID du pixel Facebook/Meta
  TURNSTILE_SITE_KEY      Clé publique Cloudflare Turnstile
  TURNSTILE_ENABLED       Activer Turnstile (true/false)
  TURNSTILE_THEME         Thème Turnstile (auto/light/dark)
  TURNSTILE_SIZE          Taille Turnstile (normal/compact)

Exemple d'utilisation:
  # Créer un fichier .env avec vos valeurs
  cp .env.example .env

  # Éditer .env avec vos vraies valeurs
  nano .env

  # Injecter dans le thème
  node analytics-env-injector.js inject
`);
}

// CLI
const command = process.argv[2] || "inject";

switch (command) {
  case "inject":
    validateEnvironment();
    injectEnvVariables();
    break;

  case "validate":
    validateEnvironment();
    break;

  case "help":
  case "--help":
  case "-h":
    showHelp();
    break;

  default:
    console.error("Commande inconnue:", command);
    showHelp();
    process.exit(1);
}
